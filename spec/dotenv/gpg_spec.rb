require 'spec_helper'

describe Dotenv::Gpg do
  it 'has a version number' do
    expect(Dotenv::Gpg::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
